<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewTotalAttendance" />
        <activity android:name=".Register" />
        <activity android:name=".AddStudent" />
        <activity android:name=".StudentDetails" />
        <activity android:name=".ViewStudents2" />
        <activity android:name=".Login"></activity>
        <activity android:name=".Gender2"></activity>
        <activity android:name=".CourseDetails" />
        <activity android:name=".ViewCourses" />
        <activity android:name=".AddCourse" />
        <activity android:name=".Course_or_student" />
        <activity android:name=".Teststatus" />
        <!--
 <activity
            android:name=".Teststatus"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Dialog" >
            ..........
        </activity>
        -->
        <activity android:name=".ViewStudents" />
        <activity android:name=".admin">

        </activity>
    </application>

</manifest>